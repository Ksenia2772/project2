RewriteEngine On
RewriteBase /

# Перенаправляем все API запросы на api.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/.*$ api.php [QSA,L]

# Если файла нет, но это не API - 404